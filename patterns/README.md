# Patterns

Short notes on where a block sits and what fails without it. Add pages only when a video or Insight Veda case needs a durable name.

See `.cursor/skills/maintain-hub/templates.md` for the pattern template.
